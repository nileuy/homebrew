# The Crooked Moon — Player Options (TCM_) for 5e.tools

Use the homebrew importer in 5e.tools with `TCM__homebrew_bundle.json`.

Files use `"source": "TCM_"` consistently and link internally with `|TCM_|` in tags.
